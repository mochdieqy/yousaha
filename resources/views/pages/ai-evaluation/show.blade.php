@extends('layouts.home')

@section('title', $evaluation->title)

@section('content')
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="page-title-box d-flex align-items-center justify-content-between">
                <h4 class="mb-0">{{ $evaluation->title }}</h4>
                <div class="page-title-right">
                    <a href="{{ route('ai-evaluation.index') }}" class="btn btn-secondary mb-3">
                        <i class="fas fa-arrow-left"></i> Back to Evaluations
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">Evaluation Details</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <table class="table table-borderless">
                                <tr>
                                    <td><strong>Category:</strong></td>
                                    <td><span class="badge bg-info">{{ $evaluation->category_display_name }}</span></td>
                                </tr>
                                <tr>
                                    <td><strong>Period:</strong></td>
                                    <td>{{ $evaluation->period_display }}</td>
                                </tr>
                                <tr>
                                    <td><strong>Status:</strong></td>
                                    <td>
                                        @if($evaluation->isCompleted())
                                            <span class="badge bg-success">Completed</span>
                                        @elseif($evaluation->isDraft())
                                            <span class="badge bg-warning">Draft</span>
                                        @else
                                            <span class="badge bg-danger">Failed</span>
                                        @endif
                                    </td>
                                </tr>
                                <tr>
                                    <td><strong>Generated By:</strong></td>
                                    <td>{{ $evaluation->generatedByUser->name ?? 'N/A' }}</td>
                                </tr>
                                <tr>
                                    <td><strong>Generated On:</strong></td>
                                    <td>{{ $evaluation->created_at->format('M d, Y H:i') }}</td>
                                </tr>
                            </table>
                        </div>
                        <div class="col-md-6">
                            <div class="d-flex justify-content-end">
                                @if($evaluation->isDraft())
                                <a href="{{ route('ai-evaluation.edit', $evaluation) }}" class="btn btn-warning me-2">
                                    <i class="fas fa-edit"></i> Edit
                                </a>
                                @endif
                                

                                
                                <form action="{{ route('ai-evaluation.destroy', $evaluation) }}" method="POST" class="d-inline">
                                    @csrf
                                    @method('DELETE')
                                    <button type="submit" class="btn btn-danger" 
                                            onclick="return confirm('Are you sure you want to delete this evaluation?')">
                                        <i class="fas fa-trash"></i> Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if($evaluation->isCompleted())
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">AI-Generated Insights</h5>
                </div>
                <div class="card-body">
                    <div class="ai-content">
                        {!! nl2br(e($evaluation->content)) !!}
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if($evaluation->insights && count($evaluation->insights) > 0)
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-lightbulb text-warning"></i> Key Insights
                    </h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        @foreach($evaluation->insights as $insight)
                        <li class="list-group-item">
                            <i class="fas fa-check-circle text-success me-2"></i>
                            {{ $insight }}
                        </li>
                        @endforeach
                    </ul>
                </div>
            </div>
        </div>
    </div>
    @endif

    @if($evaluation->recommendations && count($evaluation->recommendations) > 0)
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-star text-primary"></i> Recommendations
                    </h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        @foreach($evaluation->recommendations as $recommendation)
                        <li class="list-group-item">
                            <i class="fas fa-arrow-right text-primary me-2"></i>
                            {{ $recommendation }}
                        </li>
                        @endforeach
                    </ul>
                </div>
            </div>
        </div>
    </div>
    @endif

    @if($evaluation->data_summary)
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-chart-bar text-info"></i> Data Summary
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        @if(isset($evaluation->data_summary['summary']))
                        <div class="col-md-6">
                            <h6>Summary Statistics</h6>
                            <div class="table-responsive">
                                <table class="table table-sm table-bordered">
                                    <tbody>
                                        @foreach($evaluation->data_summary['summary'] as $key => $value)
                                        <tr>
                                            <td><strong>{{ ucwords(str_replace('_', ' ', $key)) }}:</strong></td>
                                            <td>
                                                @if(is_numeric($value))
                                                    @if(strpos($key, 'rate') !== false || strpos($key, 'percentage') !== false)
                                                        {{ number_format($value, 2) }}%
                                                    @else
                                                        {{ number_format($value) }}
                                                    @endif
                                                @else
                                                    {{ $value }}
                                                @endif
                                            </td>
                                        </tr>
                                        @endforeach
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        @endif

                        @if(isset($evaluation->data_summary['monthly_trends']) && count($evaluation->data_summary['monthly_trends']) > 0)
                        <div class="col-md-6">
                            <h6>Monthly Trends</h6>
                            <div class="table-responsive">
                                <table class="table table-sm table-bordered">
                                    <thead>
                                        <tr>
                                            <th>Month</th>
                                            @foreach(array_keys($evaluation->data_summary['monthly_trends'][0]) as $header)
                                                @if($header !== 'month')
                                                    <th>{{ ucwords(str_replace('_', ' ', $header)) }}</th>
                                                @endif
                                            @endforeach
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach($evaluation->data_summary['monthly_trends'] as $trend)
                                        <tr>
                                            <td>{{ $trend['month'] }}</td>
                                            @foreach($trend as $key => $value)
                                                @if($key !== 'month')
                                                    <td>
                                                        @if(is_numeric($value))
                                                            {{ number_format($value) }}
                                                        @else
                                                            {{ $value }}
                                                        @endif
                                                    </td>
                                                @endif
                                            @endforeach
                                        </tr>
                                        @endforeach
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        @endif
                    </div>
                </div>
            </div>
        </div>
    </div>
    @endif

    @elseif($evaluation->isFailed())
    <div class="row">
        <div class="col-12">
            <div class="card mb-4">
                <div class="card-body text-center">
                    <div class="text-danger mb-3">
                        <i class="fas fa-exclamation-triangle fa-3x"></i>
                    </div>
                    <h5>Evaluation Generation Failed</h5>
                    <p class="text-muted">The AI evaluation could not be generated. Please try again or contact support.</p>
                    <form action="{{ route('ai-evaluation.regenerate', $evaluation) }}" method="POST">
                        @csrf
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-sync-alt"></i> Try Again
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    @endif
</div>
@endsection

@section('script')
<script>
$(document).ready(function() {
    // Initialize any additional JavaScript functionality here
});
</script>
@endsection
