@extends('layouts.home')

@section('title', 'AI Evaluation')

@section('content')
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="page-title-box d-flex align-items-center justify-content-between">
                <h4 class="mb-0">AI Evaluation</h4>
                <div class="page-title-right">
                    <a href="{{ route('ai-evaluation.create') }}" class="btn btn-primary mb-3">
                        <i class="fas fa-plus"></i> Generate New Evaluation
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>Title</th>
                                    <th>Category</th>
                                    <th>Period</th>
                                    <th>Status</th>
                                    <th>Generated By</th>
                                    <th>Created</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @forelse($evaluations as $evaluation)
                                <tr>
                                    <td>
                                        <a href="{{ route('ai-evaluation.show', $evaluation) }}" class="text-decoration-none">
                                            {{ $evaluation->title }}
                                        </a>
                                    </td>
                                    <td>
                                        <span class="badge bg-info">{{ $evaluation->category_display_name }}</span>
                                    </td>
                                    <td>{{ $evaluation->period_display }}</td>
                                    <td>
                                        @if($evaluation->isCompleted())
                                            <span class="badge bg-success">Completed</span>
                                        @elseif($evaluation->isDraft())
                                            <span class="badge bg-warning">Draft</span>
                                        @else
                                            <span class="badge bg-danger">Failed</span>
                                        @endif
                                    </td>
                                    <td>{{ $evaluation->generatedByUser->name ?? 'N/A' }}</td>
                                    <td>{{ $evaluation->created_at->format('M d, Y H:i') }}</td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a href="{{ route('ai-evaluation.show', $evaluation) }}" 
                                               class="btn btn-sm btn-outline-primary" 
                                               title="View">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            
                                            @if($evaluation->isDraft())
                                            <a href="{{ route('ai-evaluation.edit', $evaluation) }}" 
                                               class="btn btn-sm btn-outline-warning" 
                                               title="Edit">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            @endif
                                            

                                            
                                            <form action="{{ route('ai-evaluation.destroy', $evaluation) }}" 
                                                  method="POST" 
                                                  class="d-inline">
                                                @csrf
                                                @method('DELETE')
                                                <button type="submit" 
                                                        class="btn btn-sm btn-outline-danger" 
                                                        title="Delete"
                                                        onclick="return confirm('Are you sure you want to delete this evaluation?')">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                @empty
                                <tr>
                                    <td colspan="7" class="text-center text-muted">
                                        <div class="py-4">
                                            <i class="fas fa-robot fa-3x mb-3"></i>
                                            <h5>No AI Evaluations Yet</h5>
                                            <p>Start generating AI-powered insights for your business data.</p>
                                            <a href="{{ route('ai-evaluation.create') }}" class="btn btn-primary">
                                                Generate First Evaluation
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection

@section('script')
<script>
$(document).ready(function() {
    // Initialize any additional JavaScript functionality here
});
</script>
@endsection
